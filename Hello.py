class Product():
    def __init__(self, name, price,tax):
        self.name = name
        self.price = price

class Store()
    def __init__(self, name, address,discount):
        self.name = name
        self.address = address

class calculation

def calculateTotalPrice(Quantity,Price,Tax,discount)
    return quantity*price(1+(tax/100))*(1-(discount/100))


Class Billing import product, store, calculations
s1=
p1=
p2=
p3=

p1cost= calculateTotalPrice(1,p1.price,p1.tax,s1.discount)
p1cost= calculateTotalPrice(1,p1.price,p1.tax,s1.discount)
p1cost= calculateTotalPrice(1,p1.price,p1.tax,s1.discount)

print (s1.name)
print (p1.name,p1.quantity, p1.tax, p1,cost)
print (p1.name,p1.quantity, p1.tax, p1,cost)
print (p1.name,p1.quantity, p1.tax, p1,cost)
print (p1.name,p1.quantity, p1.tax, p1,cost)
